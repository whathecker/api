let {
    listAddressesByUserId,
    addAddress,
    updateAddress,
    deleteAddressById,
    dropAll
} = require('./memory');

module.exports = {
    listAddressesByUserId,
    addAddress,
    updateAddress,
    deleteAddressById,
    dropAll
};